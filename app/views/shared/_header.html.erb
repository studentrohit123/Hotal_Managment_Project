<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Hotal Managment Portal</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Hotels', root_path, class: 'nav-link active', 'aria-current': 'page' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Booking', bookings_path, class: 'nav-link active', 'aria-current': 'page' %>
        </li>
      </ul>
      <form class="d-flex" role="search">
      <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
          <%= link_to "Logout", users_sign_out_path, data: { turbo_method: :delete }, class: 'nav-link' %>
          <% else %>
          <%= link_to " Login ", new_user_session_path, class: 'nav-link' %>
        <% end %> 
        </ul>
      </div>
    </nav>
  </header>
  </form>
    </div>
  </div>
</nav>